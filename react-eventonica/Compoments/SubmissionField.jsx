//let username = $('#add-username')[0].value;
//     let title = $('#add-title')[0].value;

//takes what goes in here and turns it into a property that can be put into SubmissionForm
const SubmissionField = (userInput) =>   {
    <input></input>
}